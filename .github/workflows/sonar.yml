name: SonarQube Scan

on:
  push:
    branches:
      - main

jobs:
  sonarQube:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: SonarQube Scan
        uses: sonarsource/sonarqube-scan-action@v2.2.0
        with:
          args: >
            -Dsonar.projectKey=Proyecto_BAR
            -Dsonar.sources=.
            -Dsonar.host.url=https://lestobiotic-salvador-unconferred.ngrok-free.dev
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_GITHUB }}
